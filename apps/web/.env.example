# Family sign-in (optional). Without these two values the app runs with no sign-in
# and keeps progress on the device only. Both are safe to expose to the browser.
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_your-key
