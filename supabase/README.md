# Database

Supabase project files land here in milestone M0: `config.toml`, `migrations/`, `functions/`, `tests/`, and `seed/`.
Nothing is here yet because no Supabase project exists. See docs/tech-spec.md section 5.3.
